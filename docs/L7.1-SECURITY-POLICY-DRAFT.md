---
title: Security Policy
slug: security
category: Security & Compliance
version: "1.0"
effectiveDate: ""
lastUpdated: "2026-07-18"
status: draft
owner: Security Lead
requiresCounselReview: true
requiresReConsent: false
relatedPolicies: [privacy, data-retention, biometric-data, identity-verification, vulnerability-disclosure]
description: Security principles, access controls, and safeguards for the Tirvea platform.
---
# Tirvea - Security Policy

**MASTER LEGAL DRAFT - L7.1**

> **Status: DRAFT for professional legal review. Not yet published.**
> Prepared as an internal master draft for review by security and legal counsel. It must be read consistently with the L1 Legal Architecture and with the L2.2 Terms of Service, L2.3 Privacy Policy, L4.1 Data Retention Policy, L5.1 Biometric Information Policy, L5.2 Photo Verification Policy, L5.3 Identity Verification Policy, and the Trust and Safety Policy. Items that are provider-managed, configuration-dependent, or not implemented in the codebase are marked **[Legal review required before publication.]**

**Ownership note.** This Security Policy owns **security practices only**: security principles, identity and access management, authentication, authorization, infrastructure and cloud security, application security, data-protection safeguards, logging and monitoring, and incident response. It is **not** the Privacy Policy (`/legal/privacy`), the Biometric Information Policy (`/legal/biometric-data`), or the Trust and Safety Policy. It references, but does not restate, those.

---

## Document control

| Field | Value |
|---|---|
| **Document** | Security Policy |
| **Contracting entity** | WiseWave Limited |
| **Company number** | 762171 (Companies Registration Office, Ireland) |
| **Registered office** | 39 Cooley Park, Dundalk, Co. Louth, A91 AP2V, Ireland |
| **Contact** | info@tirvea.com |
| **Brand / platform** | Tirvea |
| **Canonical URL** | `/legal/security` |
| **Document Version** | 1.0 (draft) |
| **Effective Date** | *[to be set on publication]* |
| **Last Updated** | 2026-07-18 |
| **Language** | English (en) |
| **Owner** | Security Lead |
| **Nature** | Information security policy. It describes safeguards actually implemented and clearly flags provider-managed and unimplemented items. |

**Entity convention (binding on all drafters):** The platform is **Tirvea**. The contracting entity is always **WiseWave Limited**. The forms *"Tirvea Ltd"* and *"Tirvea Limited"* must never appear.

**Accuracy commitment.** This Policy describes only the security controls the platform actually implements. It does not overstate coverage, does not claim certifications the platform does not hold, and flags controls that are provider-managed or not yet implemented rather than implying them.

---

## 1. Output - Repository audit

Findings from the implementation (see Output 5 for the review):

**Implemented (repository-backed):**
- **Authentication.** Identity is provided by **Supabase Auth**; Tirvea does not store account passwords. Every request is verified through Supabase `getUser()`, which checks the token signature, expiry, and revocation (`src/lib/auth.ts`). Two transports resolve to the same principal: the browser's Supabase SSR cookies and an `Authorization: Bearer` access token for API clients; when both are present they are cross-checked and a mismatch is rejected.
- **Authorization.** Access control is **explicit role-based permissions** (`src/lib/rbac.ts`, `hasPermission(role, permission)`). API routes go through `requireSession` / `requirePermission` (`src/lib/api.ts`), returning `401 unauthorized` or `403 forbidden`; restricted accounts are gated with `403 account_restricted`. Responses use a strict `{ data }` / `{ error }` envelope.
- **IAM and least privilege (AWS).** Browser Face Liveness uses **AWS STS AssumeRole** to mint **short-lived credentials** (900 seconds by default, clamped to 900-3600) scoped to a single action, `rekognition:StartFaceLivenessSession`, on one role ARN (`src/lib/services/aws-sts.ts`). The runtime principal holds only `sts:AssumeRole` on that role; the temporary credentials can start a liveness stream and nothing else. The role session name is a salted hash (no personal data), and credential material is never logged.
- **Server-side AWS (Rekognition).** Server face operations are **SigV4-signed HTTPS** calls (no AWS SDK) pinned to **approved EU regions** (`eu-west-1`; other regions are refused) (`src/lib/services/aws-rekognition.ts`). Collection administration (Create/Delete collection) is not granted to the runtime. AWS types, ARNs, FaceIds, and similarity scores are contained in the adapter and are never logged.
- **Signed webhooks.** All four webhook endpoints verify a signature over the **raw request body** with **timing-safe comparison** before processing: Stripe billing and Stripe Identity (`verifyStripeSignature`, HMAC-SHA256; `src/lib/webhook-signatures.ts`), Resend email (Svix `svix-id`/`svix-timestamp`/`svix-signature`), and Supabase auth (`x-webhook-secret` compared with `secretsEqual`). Unsigned or badly-signed deliveries are rejected with `401`; processed Stripe events are recorded and de-duplicated to resist replay.
- **Cryptography and hashing.** HMAC-SHA256 with `timingSafeEqual` for webhook signatures; **salted SHA-256** for IP, user-agent, and device hashes so raw IPs and user agents are never stored (`src/lib/auth/audit.ts`); `randomUUID` for device and correlation identifiers. Account credentials are handled by Supabase, so no application-managed password hashing exists.
- **Secrets.** Secrets are read from environment variables, trimmed at read, and never logged; the health check names variables only, never values (`/api/health`), `poweredByHeader` is disabled, and there are no secrets committed to the repository.
- **Rate limiting.** A **distributed fixed-window** limiter (Upstash Redis over its REST API, with a per-instance in-memory fallback) protects sensitive actions (`src/lib/rate-limit.ts`). Keys are `action:principal` where the principal is a user id or an **IP hash**; logs carry only the action segment. It is applied across authentication send/verify, discovery, swipes, first messages, and explore routes; exceeded limits return `429`.
- **Application security and API protection.** Input is validated with Zod; billing uses Stripe idempotency keys and an `Idempotency-Key` replay mechanism; media is served through **signed, short-TTL URLs** (Supabase Storage `createSignedUrl`); push endpoints validate destinations to prevent server-side request forgery and never carry chat text in the payload; the API is exposed on a versioned `/api/v1` surface.
- **Security headers.** Set globally in `next.config.ts`: **HSTS** (`max-age=63072000; includeSubDomains; preload`), `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, and `Permissions-Policy: camera=(), microphone=(), geolocation=(self)`.
- **Logging, monitoring, and audit.** Observability logs carry **safe machine codes only** - never tokens, never principals, rejection reasons only. Append-only audit trails exist for privileged and safety actions (`AdminLog`), authentication events with hashed IP/user-agent (`AuthVerificationEvent`), and verification events (`VerificationAuditEvent`). A health endpoint (`/api/health`) reports database, configuration, and billing status; a monitoring runbook defines operator dashboards and alerting.

**Provider-managed / not implemented / flagged:**
- **No Content-Security-Policy header** is currently set among the security headers. **[Legal review required before publication - confirm whether CSP is delegated to the platform or is a gap.]**
- **Encryption at rest** is **provider-managed** (Supabase Postgres and Storage, Stripe, AWS); Tirvea runs no datastore of its own. **[Legal review required before publication - confirm the at-rest encryption position with each provider.]**
- **Backups and point-in-time recovery** are **provider-managed** (Supabase); there is no backup code in the repository. Retention specifics belong to the Data Retention Policy. **[Legal review required before publication - confirm backup and recovery arrangements.]**
- **External error-monitoring / SIEM** (for example a hosted error tracker) is **operator-provisioned**; the codebase emits structured logs and a health endpoint but wires no specific third-party monitoring service. **[Legal review required before publication - confirm the production monitoring stack.]**
- **Face-match layer dormancy.** The AWS Rekognition **face-collection** layer (index/search/delete) is **dormant by default** (master switch off; a mock provider is used; a legal gate and internal allowlist apply). The live verification path is Stripe Identity. This is covered by the Biometric Information, Identity Verification, and Photo Verification policies. **[Legal review required before publication.]**

## 2. Output - Table of Contents

1. Purpose
2. Scope
3. Security Principles
4. Identity and Access Management
5. Authentication
6. Authorization
7. Infrastructure Security
8. Cloud Security
9. Application Security
10. Data Protection
11. Logging and Monitoring
12. Incident Response
13. Third-Party Providers
14. User Responsibilities
15. Policy Updates
16. Contact

## 3. Output - Document architecture

| Aspect | Decision |
|---|---|
| **Owns** | Security principles and practices: IAM, authentication, authorization, infrastructure, cloud, application, data-protection safeguards, logging/monitoring, incident response. |
| **References (does not restate)** | Privacy Policy (`/legal/privacy`) for lawful basis and data-subject rights; Data Retention Policy (`/legal/data-retention`) for periods and backups; Biometric Information Policy (`/legal/biometric-data`), Identity Verification Policy (`/legal/identity-verification`), and Photo Verification Policy (`/legal/photo-verification`) for verification specifics; Vulnerability Disclosure Policy (`/legal/vulnerability-disclosure`) for reporting. |
| **Honesty rule** | Only implemented controls are stated as implemented; provider-managed and unimplemented controls are labelled as such. No certification is claimed. |

## 4. Output - Complete Security Policy

> **Reading note.** This Policy explains how WiseWave Limited secures the Tirvea platform. It covers security practices only. How your personal data is used is in the Privacy Policy (`/legal/privacy`); biometric and verification specifics are in their own policies; to report a vulnerability, see the Vulnerability Disclosure Policy (`/legal/vulnerability-disclosure`).

### 1. Purpose

**Purpose.** To describe the information-security principles, technical safeguards, and operational controls WiseWave Limited applies to protect the Tirvea platform and the data it processes. This Policy supports, and is part of, your agreement with WiseWave Limited (Terms of Service, `/legal/terms`).

### 2. Scope

**Purpose.** To state what this Policy covers. It covers the security of the Tirvea application, its APIs, and the managed cloud services it depends on. It does not set out how personal data is used (Privacy Policy, `/legal/privacy`), the verification of identity or photos (Identity Verification and Photo Verification policies), or content moderation (Trust and Safety Policy). It applies to WiseWave Limited and its service providers acting on its behalf.

### 3. Security Principles

**Purpose.** To state the principles that guide our controls.

- **Least privilege.** Components and credentials are granted the minimum access they need; for example, browser liveness credentials are scoped to a single AWS action and expire in minutes.
- **Defence in depth.** Multiple layers apply: authenticated sessions, explicit authorization, signed webhooks, rate limiting, input validation, and security headers.
- **Secure by default.** Sensitive features stay off until explicitly enabled and permitted; secrets are never logged; error responses expose machine codes, not internal detail.
- **Data minimisation in logs.** Identifiers such as IP and user agent are stored only as salted hashes; logs never contain tokens, credentials, or personal principals.
- **Provider responsibility, clearly bounded.** Where a control is delivered by a provider (for example encryption at rest or backups), we say so rather than implying we operate it ourselves.

### 4. Identity and Access Management

**Purpose.** To explain how identities and privileged access are managed. User identity is anchored to **Supabase Auth**; a single Supabase user id is the principal across the app. Access to protected resources is governed by **explicit role-based permissions**; there is no implied role hierarchy, and each permission is checked directly. For cloud access, the runtime uses **least-privileged credentials**: AWS access is limited to assuming one narrowly-scoped role for liveness streaming and to region-pinned server calls, with collection administration excluded. **[Legal review required before publication - confirm the administrative-access model for operators.]**

### 5. Authentication

**Purpose.** To explain how users are authenticated. Authentication is handled by **Supabase Auth**; Tirvea does not store account passwords. Sessions are established through Supabase and verified on every request by validating the session token's signature, expiry, and revocation status. The platform accepts both browser session cookies and bearer access tokens for API clients; when both are presented they must resolve to the same user. Sensitive authentication actions (such as sending or verifying one-time codes) are **rate limited**, and authentication events are recorded with **hashed** IP and user-agent values.

### 6. Authorization

**Purpose.** To explain how access decisions are made after authentication. Every protected API route requires a valid session and, where relevant, a **specific permission** tied to the user's role. Requests without a session receive `401`; authenticated requests lacking the required permission receive `403`. Accounts under restriction are blocked at a single choke point with a `403 account_restricted` response. Authorization is explicit and enumerated, not inferred.

### 7. Infrastructure Security

**Purpose.** To describe the platform infrastructure and its protections. Tirvea runs as a **serverless web application** on managed hosting, with a managed **PostgreSQL** database and object storage (Supabase), and integrates Stripe and AWS. All traffic is served over **HTTPS/TLS**, enforced by **HSTS** (two-year `max-age`, `includeSubDomains`, `preload`). Responses set hardening headers (`X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy`, `Permissions-Policy`) and suppress the framework's identifying header. A **Content-Security-Policy** header is not currently configured and is under review. **Backups and point-in-time recovery are provider-managed.** **[Legal review required before publication - confirm CSP and backup arrangements.]**

### 8. Cloud Security

**Purpose.** To describe how cloud provider access is secured. AWS is accessed with **SigV4-signed** requests over HTTPS, pinned to **approved EU regions** (`eu-west-1`); requests targeting a non-approved region are refused. Browser liveness uses **AWS STS AssumeRole** to issue **short-lived, single-action** credentials; the assumed role permits only starting a liveness session, and the credentials expire within minutes. Server-side face operations are confined to an adapter that treats provider references as opaque and never logs images, reference handles, or scores. The AWS **face-collection** layer is **dormant by default** and gated behind configuration, a legal gate, and an internal allowlist. **[Legal review required before publication - confirm the cloud-provider configuration in production.]**

### 9. Application Security

**Purpose.** To describe safeguards in the application layer. Inputs are **validated** (schema validation) before use. Webhooks from Stripe, Resend, and Supabase are **signature-verified over the raw body** with timing-safe comparison, and processed events are de-duplicated to resist replay. Sensitive actions are **rate limited**. Billing operations use **idempotency keys**, and an idempotency-replay mechanism prevents duplicate side effects. User media is delivered through **signed, short-lived URLs**, and push notifications validate their destination and never carry message content. The public API is exposed on a **versioned** surface.

### 10. Data Protection

**Purpose.** To describe how data is protected technically. Data is protected **in transit** by TLS across all traffic, including to Supabase, Stripe, and AWS. **Encryption at rest is provided by the underlying platforms** (Supabase database and storage, Stripe, AWS). In application logs and audit records, identifiers such as IP and user agent are stored only as **salted hashes**, and sensitive material (tokens, credentials, verification images, biometric references, similarity scores) is **never logged**. How long data is kept, and how it is deleted, is set by the **Data Retention Policy** (`/legal/data-retention`); how it is used and your rights over it are in the **Privacy Policy** (`/legal/privacy`). **[Legal review required before publication - confirm the at-rest encryption position per provider.]**

### 11. Logging and Monitoring

**Purpose.** To describe logging, monitoring, and audit. Operational logs record **safe machine codes and reasons only**, never tokens or personal principals. **Append-only audit trails** record privileged and safety-relevant actions, authentication events (with hashed IP and user agent), and verification events. A **health endpoint** reports database, configuration, and billing status, and a monitoring runbook defines dashboards and alerting for operators. External error-monitoring or security-information-and-event-management tooling, where used, is **operator-provisioned**. **[Legal review required before publication - confirm the production monitoring and alerting stack.]**

### 12. Incident Response

**Purpose.** To explain how we handle security incidents. We investigate suspected security incidents, contain and remediate them, and, where a personal-data breach meets the legal threshold, we notify the competent supervisory authority and affected users **as required by applicable law** (including the GDPR), in cooperation with our providers. Our detective controls (audit trails, health checks, rate-limit and authentication signals, and provider alerting) support timely detection and response. Vulnerability reports are handled under the **Vulnerability Disclosure Policy** (`/legal/vulnerability-disclosure`). **[Legal review required before publication - confirm the incident-response runbook, roles, and notification timelines.]**

### 13. Third-Party Providers

**Purpose.** To identify the providers we rely on for security-relevant functions. Tirvea relies on **Supabase** (authentication, database, storage), **Stripe** (payments and identity verification), **Amazon Web Services** (Rekognition and STS for face liveness, EU region), **Resend** (transactional email), and **Upstash** (rate-limit store). These providers process data on our behalf as described in the **Privacy Policy** (`/legal/privacy`); certain security controls, including encryption at rest and backups, are delivered by them. We select providers on the basis of their security posture and bind them by contract. **[Legal review required before publication - confirm the provider list and the data-processing agreements.]**

### 14. User Responsibilities

**Purpose.** To explain your part in keeping your account secure. Keep control of the email address, phone number, or third-party sign-in you use to access Tirvea, and of the device you use. Do not share access to your account, and sign out on shared devices. Tell us promptly at info@tirvea.com if you believe your account has been accessed without your permission or you notice suspicious activity. Follow the Terms of Service (`/legal/terms`); do not attempt to probe, scan, or breach the platform's security except through the Vulnerability Disclosure Policy (`/legal/vulnerability-disclosure`).

### 15. Policy Updates

**Purpose.** To explain changes to this Policy. We may update this Policy as our controls and providers evolve. Where required by law, we give notice of material changes. The current version and its effective date are shown on this page. This Policy does not reduce your statutory rights, and it is read together with the Terms of Service (`/legal/terms`) and Privacy Policy (`/legal/privacy`).

### 16. Contact

**Purpose.** To tell you how to reach us on security matters.

- **Contracting entity:** WiseWave Limited (Company Number 762171)
- **Registered office:** 39 Cooley Park, Dundalk, Co. Louth, A91 AP2V, Ireland
- **Email:** info@tirvea.com

To report a security vulnerability, please use the **Vulnerability Disclosure Policy** (`/legal/vulnerability-disclosure`). For privacy questions, see the Privacy Policy (`/legal/privacy`); for the overall contract, the Terms of Service (`/legal/terms`).

---

## 5. Output - Implementation review

| Check | Result |
|---|---|
| **Entity naming** | "WiseWave Limited" as entity; "Tirvea" as brand; no forbidden entity forms. |
| **Authentication** | Supabase Auth; per-request `getUser()` validation; cookie + bearer with conflict check - matches `src/lib/auth.ts`. |
| **Authorization** | Explicit RBAC; `requireSession` / `requirePermission`; 401/403; account_restricted - matches `rbac.ts` + `api.ts`. |
| **IAM / least privilege** | STS AssumeRole, single-action scope, short TTL, hashed session name - matches `aws-sts.ts`. |
| **Cloud (Rekognition)** | SigV4, EU region pin, collection-admin excluded, no data-plane logging - matches `aws-rekognition.ts`. |
| **Signed webhooks** | Raw-body signature verification, timing-safe, replay de-dup - matches `webhook-signatures.ts` + webhook routes. |
| **Hashing / crypto** | HMAC-SHA256 + timingSafeEqual; salted SHA-256 for IP/UA/device - matches `auth/audit.ts`. |
| **Secrets** | Env-only, never logged; poweredByHeader off; health names variables only - matches `next.config.ts` + `/api/health`. |
| **Rate limiting** | Distributed fixed-window; principal is user id or IP hash; 429 - matches `rate-limit.ts`. |
| **API protection** | Zod validation, idempotency, signed media URLs, SSRF-guarded push, versioned API - matches the services. |
| **Security headers** | HSTS, X-Frame-Options, nosniff, Referrer-Policy, Permissions-Policy - matches `next.config.ts`. |
| **Logging / audit** | Safe-code logging; AdminLog / AuthVerificationEvent / VerificationAuditEvent; /api/health - matches the audit modules. |
| **CSP** | Not set - flagged. |
| **At-rest encryption / backups** | Provider-managed - flagged. |
| **External monitoring / SIEM** | Operator-provisioned - flagged. |
| **Face-match layer** | Dormant by default - flagged; deferred to the verification policies. |

**No invented security controls. Every stated control is repository-backed; CSP, at-rest encryption, backups, external monitoring, and the dormant face-match layer are flagged rather than implied.**

## 6. Output - Cross-reference validation

All referenced policies resolve to live routes in the legal registry (`src/lib/legal/registry.ts`): `/legal/privacy`, `/legal/data-retention`, `/legal/biometric-data`, `/legal/identity-verification`, `/legal/photo-verification`, `/legal/vulnerability-disclosure`, `/legal/terms`.

**No duplicated legal content:** security practices are owned here; data use and rights, retention and backups, biometric and verification specifics, and vulnerability reporting are referenced to their owning policies.

## 7. Output - Publication readiness checklist

| # | Item | Status |
|---|---|---|
| 1 | All required sections drafted (16) | Complete |
| 2 | Entity = WiseWave Limited; no forbidden entity forms | Verified |
| 3 | Company details (CRO 762171 / office / info@tirvea.com) | Verified |
| 4 | Controls repository-backed; CSP/at-rest/backups/monitoring/face-match flagged | Verified against code |
| 5 | Frontmatter complete; registry, loader, route, and footer integrated | Done (draft, noindex) |
| 6 | Cross-referenced policies resolve; no duplicated privacy/verification content | Verified |
| 7 | Typography standard (ASCII hyphen, section-range form, no doubled symbol) | Verified |
| 8 | Security review (CSP, backups, monitoring stack, incident-response runbook) | Required |
| 9 | Effective date set; flip status to published | After sign-off |

## 8. Output - Delivery report

**Delivered:** a complete master Security Policy (16 sections) describing the platform's security practices. It is grounded in the implementation: Supabase-backed authentication with per-request token validation; explicit role-based authorization; least-privileged AWS access via STS AssumeRole (single-action, short-lived) and region-pinned SigV4 Rekognition calls with no data-plane logging; raw-body, timing-safe signature verification on all webhooks with replay de-duplication; HMAC-SHA256 and salted SHA-256 hashing; env-only secret handling that never logs values; a distributed rate limiter keyed on user id or IP hash; schema validation, billing idempotency, signed short-TTL media URLs, and SSRF-guarded push; a full set of hardening headers with HSTS; and append-only audit trails plus a health endpoint and monitoring runbook.

**Gaps flagged (not invented):** no Content-Security-Policy header; encryption at rest and backups are provider-managed; external error-monitoring/SIEM is operator-provisioned; and the AWS face-collection layer is dormant by default (deferred to the verification policies). Each is marked **[Legal review required before publication.]**

**Migration:** per the task's publishing instruction, the document is wired through the Legal Publishing System - registered slug, loader mapping, thin `LegalDocument` wrapper (legacy inline page replaced), and registry metadata upgraded to a v1.0 master draft. Status remains **draft** and **noindex** pending review (not published). No application logic and no legal wording elsewhere were changed.

**Verdict:** drafting complete, internally consistent with the Privacy, Data Retention, Biometric, Identity Verification, and Photo Verification policies, and accurate to the implementation, with provider-managed and unimplemented controls clearly flagged. Not publication-ready until security review confirms the CSP position, backup and recovery arrangements, the production monitoring stack, and the incident-response runbook, and the effective date is set.
