---
title: Photo Verification Policy
slug: photo-verification
category: Verification & Biometrics
version: "1.0"
effectiveDate: ""
lastUpdated: "2026-07-17"
status: draft
owner: Privacy Counsel + Trust & Safety
requiresCounselReview: true
requiresReConsent: false
relatedPolicies: [biometric-data, identity-verification, privacy, trust-safety, appeals]
description: Confirming your profile photos belong to you.
---
# Tirvea - Photo Verification Policy

**MASTER LEGAL DRAFT - L5.2**

> **Status: DRAFT for professional legal review. Not yet published.**
> Prepared as an internal master draft for review by privacy, data-protection, and Trust & Safety counsel. The biometric handling behind photo verification is owned by the Biometric Information Policy and is referenced, not restated. Points needing confirmation are marked **[Legal review required before publication.]**. It must be read consistently with the L1 Legal Architecture and the L5.1 Biometric Information Policy, L2.3 Privacy Policy, L3.1 Trust & Safety Policy, L3.3 Account Suspension Policy, and L3.2 Appeals Policy.

**Ownership note.** This Policy explains the photo-verification workflow. It does not redefine biometric processing, which is owned by the Biometric Information Policy (`/legal/biometric-data`); it references it. Identity verification through documents (Stripe Identity) is a separate process, covered by the Identity Verification Policy (`/legal/identity-verification`).

---

## Document control

| Field | Value |
|---|---|
| **Document** | Photo Verification Policy |
| **Operator / Data Controller** | WiseWave Limited |
| **Company number** | 762171 (Companies Registration Office, Ireland) |
| **Registered office** | 39 Cooley Park, Dundalk, Co. Louth, A91 AP2V, Ireland |
| **Contact** | info@tirvea.com |
| **Brand / platform** | Tirvea |
| **Canonical URL** | `/legal/photo-verification` |
| **Document Version** | 1.0 (draft) |
| **Effective Date** | *[to be set on publication]* |
| **Last Updated** | 2026-07-17 |
| **Language** | English (en) |
| **Owner** | Privacy Counsel + Trust & Safety |
| **Nature** | Verification policy - how photo verification confirms profile authenticity. It references, but does not restate, the biometric, privacy, and enforcement policies. |

**Entity convention (binding on all drafters):** The platform is **Tirvea**. The Data Controller is always **WiseWave Limited**. The forms *"Tirvea Ltd"* and *"Tirvea Limited"* must never appear.

**Accuracy commitment.** This Policy describes only the photo-verification behaviour the platform actually implements. Where behaviour depends on configuration or is not implemented, it is flagged rather than implied.

---

## 1. Output - Repository audit

Findings from the implementation (see Output 5 for the review):

**Implemented (repository-backed):**
- **Verification session.** Photo verification runs through a short-lived **liveness session** (keyed to a flow, user, and environment) with a time-to-live; expired, consumed, or invalidated sessions are refused.
- **Liveness.** An **AWS Face Liveness** check confirms a real, live person is present. It requires active biometric consent (Biometric Information Policy, `/legal/biometric-data`).
- **Face matching.** **AWS Rekognition** compares the live capture against a reference face and returns a calibrated similarity, which is mapped by **per-provider policy thresholds** into a band (for example, match, uncertain, or no match). A face-match provider must be configured; if none is configured, matching does not run.
- **Outcomes are internal; only a boolean badge is public.** The internal outcomes and reason codes are never exposed; public surfaces show only whether an account is verified (a boolean), never a score, similarity, or reason.
- **Badge behaviour.** A successful verification issues the verified badge. A "different person" outcome **suspends** the badge; an AI or manipulation-risk outcome **withholds** the badge and is **appealable and never auto-bans**; a provider outage is treated as a provider problem, so the **previous badge is kept and the check is retried** rather than being recorded as a mismatch.
- **Attempts and retry.** Verification attempts are counted; a retry re-consumes the same liveness session where valid.
- **Risk-gated automation.** A verification risk engine bands risk (low to critical); a **critical band blocks automatic verification and forces manual review**.
- **Manual review.** A face-identity-binding review process supports human oversight, and an account can be placed in a **photo-review-required** state in which uploads are blocked until review clears.
- **Revocation.** Where verification is invalidated or consent is withdrawn, the verified status is cleared and the biometric reference is deleted (Biometric Information Policy, `/legal/biometric-data`).

**Not implemented / configuration-dependent (flagged):**
- **No fixed verification expiry.** The verified badge does not expire on a fixed calendar; the underlying biometric reference is rotated per the Biometric Information Policy, but there is no scheduled re-verification period.
- **No fixed retry cap.** Attempts are counted and subject to rate limiting; a hard maximum-attempts cap is not fixed in this draft.
- **Face-match provider activation is configuration-dependent** (as above).

Each item is reflected in the relevant section and carries a **[Legal review required before publication.]** flag.

---

## 2. Output - Table of Contents

1. Purpose
2. Scope
3. Verification Workflow
4. Verification Session
5. Selfie Verification
6. Liveness
7. Face Matching
8. Confidence and Risk Scoring
9. Verification Outcomes
10. Badge Issuance
11. Badge Revocation
12. Verification Failure
13. Retry
14. Provider Outages
15. Manual Review
16. Verification Expiration
17. Appeals
18. Security
19. Privacy
20. Children
21. Updates
22. Contact

---

## 3. Output - Document architecture

Five logical parts. This architecture is presentational - the operative document is the numbered sequence in §2.

| Part | Sections | Purpose |
|---|---|---|
| **A. Framework** | 1-3 | Purpose, scope, and the workflow overview. |
| **B. The Steps** | 4-9 | Session, selfie, liveness, matching, scoring, and outcomes. |
| **C. Badge & Errors** | 10-16 | Issuance, revocation, failure, retry, outages, review, and expiry. |
| **D. Redress** | 17 | Appeals. |
| **E. Safeguards & Governance** | 18-22 | Security, privacy, children, updates, and contact. |

**Referenced policies (never duplicated):** Terms of Service (`/legal/terms`), Privacy Policy (`/legal/privacy`), Biometric Information Policy (`/legal/biometric-data`), Identity Verification Policy (`/legal/identity-verification`), Trust & Safety Policy (`/legal/trust-safety`), Account Suspension Policy (`/legal/account-suspension`), Appeals Policy (`/legal/appeals`), AI Moderation Policy (`/legal/ai-moderation`), Data Retention Policy (`/legal/data-retention`), Security Policy (`/legal/security`), Child Safety Policy (`/legal/child-safety`).

---

## 4. Output - Complete Photo Verification Policy

> **Reading note.** This Policy explains how photo verification works, what the verified badge means, and what happens on failure, retry, review, and appeal. The biometric detail (templates, storage, deletion, consent) is owned by the Biometric Information Policy (`/legal/biometric-data`).

### 1. Purpose

**Purpose.** To explain how Tirvea confirms that the person using an account is the live person shown in the account's profile photographs, and what the resulting verified badge means. Photo verification reduces impersonation and fake profiles.

### 2. Scope

**Purpose.** To state what this Policy covers. It covers photo verification (liveness and facial comparison using AWS). It does not cover identity verification through documents (Stripe Identity), which is covered by the Identity Verification Policy (`/legal/identity-verification`), or the biometric handling, which is owned by the Biometric Information Policy (`/legal/biometric-data`).

### 3. Verification Workflow

**Purpose.** To give the overall flow. Photo verification: (1) starts a verification session; (2) captures a live selfie; (3) runs a liveness check; (4) compares your live capture against your reference face; (5) produces an internal outcome; and (6) issues, withholds, or suspends the verified badge accordingly. A risk assessment can route a case to manual review at any point.

### 4. Verification Session

**Purpose.** To explain the session. Verification runs through a **short-lived session** keyed to your verification flow, account, and environment, with a time-to-live. Expired, already-consumed, or invalidated sessions are refused, so a session cannot be reused or replayed.

### 5. Selfie Verification

**Purpose.** To explain the live capture. You complete a **live selfie capture** during the session. The capture is used for the liveness check and the facial comparison; short capture media exists only transiently in the provider's environment and is not retained by Tirvea (Biometric Information Policy, `/legal/biometric-data`).

### 6. Liveness

**Purpose.** To explain the liveness check. An **AWS Face Liveness** check confirms a real, live person is present, to prevent spoofing with a photo, mask, or screen. Liveness requires your active biometric consent; without it, the check does not run.

### 7. Face Matching

**Purpose.** To explain the comparison. **AWS Rekognition** compares your live capture against your reference face and returns a calibrated similarity. That similarity is mapped by **policy thresholds** (set per provider) into a band - such as match, uncertain, or no match - which drives the outcome. The raw similarity is never shown to users. A face-match provider must be configured for matching to run.

### 8. Confidence and Risk Scoring

**Purpose.** To explain scoring and risk. Two things inform the outcome: the **match band** (from the similarity thresholds, §7) and a **risk band** from the verification risk engine (low, medium, high, or critical). A **critical risk band blocks automatic verification and forces manual review** (§15). These signals are internal; the risk engine outputs only a band and normalised signal names (Trust & Safety Policy, `/legal/trust-safety`, and AI Moderation Policy, `/legal/ai-moderation`).

### 9. Verification Outcomes

**Purpose.** To explain outcomes, without exposing internals. Verification produces an internal outcome and reason code that are **never made public**; public surfaces show only whether an account is verified. Outcomes include a successful match (badge issued), a different-person result (badge suspended, §11), an AI or manipulation-risk result (badge withheld, appealable, §17), and a provider-unavailable result (previous badge kept, retried, §14).

### 10. Badge Issuance

**Purpose.** To explain the badge. A successful verification issues a **verified badge** that other members can see. The badge is a simple boolean indicator; it does not reveal your images, your similarity score, or any reason code. It reflects only current, valid verification.

### 11. Badge Revocation

**Purpose.** To explain revocation and suspension. The badge is **suspended** where a check indicates a different person, and **revoked and cleared** where verification is invalidated, found wrongful on review, or your biometric consent is withdrawn; on revocation the associated biometric reference is deleted (Biometric Information Policy, `/legal/biometric-data`) and, where relevant, the account state is handled under the Account Suspension Policy (`/legal/account-suspension`).

### 12. Verification Failure

**Purpose.** To explain what happens on failure. If liveness or the face match does not pass, verification does not succeed and the badge is not issued (or is suspended, §11). A failure that is a genuine mismatch is distinguished from a **provider outage** (§14), which is not treated as a failure. You can retry (§13), and you can appeal a consequential decision (§17).

### 13. Retry

**Purpose.** To explain retry. You can **retry** verification. Attempts are counted, and a retry re-uses the same valid liveness session where possible. Retries are subject to rate limiting to protect the Service. A hard maximum-attempts cap is not fixed in this draft. **[Legal review required before publication - confirm whether a maximum-attempts cap is required.]**

### 14. Provider Outages

**Purpose.** To explain resilience. If a verification provider is unavailable, this is treated as a **provider problem, not a mismatch**: your **previous badge is kept** and the check is retried, rather than recording a rejection. This ensures an outage does not wrongly remove a member's verified status.

### 15. Manual Review

**Purpose.** To explain human review. A verification can be routed to **human review** - for example, when the risk band is critical (§8) or a binding needs confirmation. During review, an account may be placed in a **photo-review-required** state in which new uploads are blocked until review clears. Reviewers act on outcomes and references, not on stored images or scores.

### 16. Verification Expiration

**Purpose.** To explain expiry. The verified badge **does not expire on a fixed calendar** in the current implementation. The underlying biometric reference is rotated periodically as described in the Biometric Information Policy (`/legal/biometric-data`), but there is no scheduled re-verification period. **[Legal review required before publication - confirm whether a re-verification period is required.]**

### 17. Appeals

**Purpose.** To explain how to challenge an outcome. If verification is withheld, suspended, or revoked and you believe that is wrong, you can **re-attempt** verification and, where a decision affects your account, **appeal** under the Appeals Policy (`/legal/appeals`). An appeal against an automated outcome is reviewed by a person.

### 18. Security

**Purpose.** To explain protection. Verification is protected by the safeguards in the Security Policy (`/legal/security`) and Biometric Information Policy (`/legal/biometric-data`): EU-region processing, opaque references, and no storage of images, geometry, or similarity scores in our systems or logs. Sessions are short-lived and single-use.

### 19. Privacy

**Purpose.** To connect to privacy. How verification data is processed is described in the Privacy Policy (`/legal/privacy`) and Biometric Information Policy (`/legal/biometric-data`); retention and deletion are governed by the Data Retention Policy (`/legal/data-retention`) and Biometric Information Policy. Only a boolean badge is public; internal outcomes and scores are not disclosed.

### 20. Children

**Purpose.** To confirm the adults-only position. Tirvea is strictly for adults (18+). Verification supports keeping the community adult and genuine; child-safety matters are governed by the Child Safety Policy (`/legal/child-safety`).

### 21. Updates

**Purpose.** To explain changes. We update this Policy to reflect changes in the verification workflow or the law - including if we introduce a re-verification period or a maximum-attempts cap. We update the "Last Updated" date and take reasonable steps to communicate material changes.

### 22. Contact

**Purpose.** To tell you how to reach us about photo verification.

- **Data Controller:** WiseWave Limited (Company Number 762171)
- **Registered office:** 39 Cooley Park, Dundalk, Co. Louth, A91 AP2V, Ireland
- **Email:** info@tirvea.com

For the biometric handling, see the Biometric Information Policy (`/legal/biometric-data`); for identity verification, the Identity Verification Policy (`/legal/identity-verification`); for appeals, the Appeals Policy (`/legal/appeals`).

---

## 5. Output - Implementation consistency review

| Check | Result |
|---|---|
| **Entity naming** | "WiseWave Limited" as Controller; "Tirvea" as brand; no forbidden entity forms. |
| **Session** | Short-lived, single-use liveness session with TTL - matches `face-liveness.ts`. |
| **Liveness** | AWS Face Liveness, consent-gated - matches `face-liveness.ts`. |
| **Face matching / thresholds** | AWS Rekognition similarity mapped to bands by per-provider policy thresholds - matches `face-match-providers.ts`. |
| **Only boolean badge public** | Internal outcomes/reason codes never exposed - matches `face-outcomes.ts` (isPubliclyVerified). |
| **Badge behaviour** | Different-person suspends; AI/manipulation withholds (appealable, no auto-ban); provider-unavailable keeps badge + retries - matches `face-outcomes.ts`. |
| **Attempts / retry** | Attempts counted; retry re-consumes the same session - matches `face-liveness.ts` / `face-verification.ts`. |
| **Critical risk forces manual review** | Matches the risk engine. |
| **Photo-review-required state** | Uploads blocked until review - matches `trust-safety.ts`. |
| **Revocation** | Clears verified status + deletes reference - matches L5.1 and the revocation flow. |
| **No fixed expiry / no fixed retry cap** | Flagged, not invented. |
| **No duplicated biometric definitions** | Biometric handling referenced to the Biometric Information Policy. |

**No invented functionality; no overstated capability. Every claim is repository-backed; expiry and retry-cap items are flagged.**

## 6. Output - Cross-reference validation

All referenced policies resolve to live routes in the legal registry (`src/lib/legal/registry.ts`): `/legal/terms`, `/legal/privacy`, `/legal/biometric-data`, `/legal/identity-verification`, `/legal/trust-safety`, `/legal/account-suspension`, `/legal/appeals`, `/legal/ai-moderation`, `/legal/data-retention`, `/legal/security`, `/legal/child-safety`.

**No duplicated legal definitions:** the verification workflow is owned here; biometric handling, data-processing detail, retention, and enforcement states are referenced to their owning policies.

## 7. Output - Publication readiness checklist

| # | Item | Status |
|---|---|---|
| 1 | All sections drafted (22) | Complete |
| 2 | Controller = WiseWave Limited; no forbidden entity forms | Verified |
| 3 | Company details (CRO 762171 / office / info@tirvea.com) | Verified |
| 4 | Workflow repository-backed; expiry / retry-cap items flagged | Verified against code |
| 5 | Frontmatter complete; registry, loader, route, and footer integrated | Done (draft, noindex) |
| 6 | Cross-referenced policies resolve; no duplicated definitions | Verified |
| 7 | Typography standard (ASCII hyphen, §10-§15 form, no doubled symbol) | Verified |
| 8 | Counsel review of flagged items (expiry, retry cap) | Required |
| 9 | Effective date set; flip status to published | After sign-off |

## 8. Output - Delivery report

**Delivered:** a complete master Photo Verification Policy (22 sections) that explains the real verification workflow - a short-lived single-use liveness session, AWS Face Liveness, AWS Rekognition face matching with per-provider threshold bands, internal outcomes with only a boolean verified badge exposed, badge issuance/suspension/withholding/revocation, retry with counted attempts, provider-outage resilience (badge kept, retried), risk-gated manual review with a photo-review-required state, and revocation that deletes the biometric reference. It references, and does not restate, the Biometric Information Policy (which owns the biometric handling) and keeps identity verification (Stripe Identity) separate. Verification expiry and a maximum-attempts cap are flagged as not fixed.

**Migration:** per the task's publishing instruction, the document is wired through the Legal Publishing System - registered slug, loader mapping, thin `LegalDocument` wrapper (legacy page replaced), and registry metadata upgraded to a v1.0 master draft. Status remains **draft** and **noindex** pending counsel approval (not published). No application logic and no legal wording elsewhere were changed.

**Verdict:** drafting complete, internally consistent with L5.1 and the enforcement and privacy policies, and accurate to the implementation, with the expiry and retry-cap items flagged. Not publication-ready until counsel confirms those items and the effective date.
